---
import { labNav } from '../lib/nav';
import type { SiteSettings } from '../lib/types';

type Props = {
  settings: SiteSettings;
  current?: boolean;
  /** `m` = フッター等の標準、`s` = FV 画像上のコンパクト版 */
  size?: 'm' | 's';
  class?: string;
};

const { settings, current = false, size = 'm', class: className } = Astro.props;
---

<a
  class:list={[
    'lab-banner',
    size === 's' && 'lab-banner--s',
    current && 'lab-banner--current',
    className,
  ]}
  href={labNav.href}
  aria-current={current ? 'page' : undefined}
>
  <span class="lab-banner__content">
    <span class="lab-banner__eyebrow">技術インフォメーション</span>
    <span class="lab-banner__title">{settings.labName}</span>
    {size !== 's' && (
      <span class="lab-banner__text">技術記事一覧のページへ</span>
    )}
  </span>
  <span class="lab-banner__arrow" aria-hidden="true">→</span>
</a>
