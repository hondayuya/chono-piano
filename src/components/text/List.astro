---
type Props = {
  /** `ul` (default) or `ol`. */
  as?: 'ul' | 'ol';
  class?: string;
};

const { as = 'ul', class: className } = Astro.props;
const Tag = as;
---

<Tag
  class:list={[
    'text-list',
    as === 'ol' && 'text-list--ordered',
    className,
  ]}
>
  <slot />
</Tag>
